# ensure key file is readable only by you
chmod 400 ~/Downloads/labsuser.pem

# copy local file to /home/ec2-user on the bastion
scp -i ~/Downloads/labsuser.pem ~/Downloads/labsuser2.pem ec2-user@bastion-host-public-ip:/home/ec2-user      

#cat /etc/os-release

# For Amazon Linux 2 / 2023, RHEL, or CentOS - 
-sudo dnf install -y mariadb105   - sudo yum install -y mariadb105(old) 
                                                                                                                                                                                 For Ubuntu / Debian - sudo apt updaqte 
sudo apt install -y mariadb105-client

# mysql --version

# mysql -h ... -u ... -p ...

# sudo yum install -y mysql failed because the default repo doesn’t include the MySQL client package — on Amazon Linux 2 or 2023, it lives in the extra repositories:
sudo amazon-linux-extras enable mariadb10.5
sudo yum clean metadata  
sudo yum install -y mariadb                                                                                          


         